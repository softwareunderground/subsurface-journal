############
Installation
############

`Download <https://www.anaconda.com/distribution/>`_
and install the Python Anaconda Distribution.

.. code-block:: shell

    conda env create -n environment.yml
    source activate journal
    
    python -m ipykernel install --user --name journal --display-name "Python (journal)"

